import React, {Component} from "react"
import Typography from "@material-ui/core/Typography";

const PageTitle = (props) => <Typography variant={"h3"}>{props.children}</Typography>

export {PageTitle}